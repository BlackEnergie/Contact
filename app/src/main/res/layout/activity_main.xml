<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="fr.sio.contacts.MainActivity">

    <Button
        android:id="@+id/btnProfil"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Mon profil"
        android:layout_marginTop="51dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnContacts"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Mes contacts"
        android:layout_marginTop="20dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/btnProfil"
        app:layout_constraintLeft_toLeftOf="@+id/btnProfil" />

    <Button
        android:id="@+id/btnAjouterContact"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Ajouter Contact"
        android:layout_marginTop="20dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/btnContacts"
        app:layout_constraintLeft_toLeftOf="@+id/btnContacts" />


    <Button
        android:id="@+id/btnMesGroupes"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Mes Groupes"
        android:layout_marginTop="20dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/btnAjouterContact"
        app:layout_constraintLeft_toLeftOf="@+id/btnAjouterContact" />

    <Button
        android:id="@+id/btnAjouterGroupe"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Ajouter Groupe"
        android:layout_marginTop="20dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/btnMesGroupes"
        app:layout_constraintLeft_toLeftOf="@+id/btnMesGroupes" />

    </android.support.constraint.ConstraintLayout>